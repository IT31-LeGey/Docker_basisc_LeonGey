# Docker-Compose-Datei für Watchtower (https://hub.docker.com/r/containrrr/watchtower)
version: '3.8'
services:
  watchtower:
    image: containrrr/watchtower
    container_name: watchtower
    restart: always
    volumes:
      # Dies ist notwendig, um auf den Docker Daemon zuzugreifen und Container zu steuern
      - /var/run/docker.sock:/var/run/docker.sock
      
    # Metrik-Ports sind entfernt
    # ports:
    #   - "8080:8080" 
      
    # Das fehlerverursachende --metrics-addresse ist entfernt
    command: --cleanup